// TEMPLATE: Rename to secrets.h and put your real values there OR nutze das WebUI.
// Wichtig: füge secrets.h zu .gitignore hinzu, damit keine Secrets committed werden.

#ifndef SECRETS_H
#define SECRETS_H

// Wenn du defaults per Build möchtest, trage sie hier ein.
// Für das Web-Setup leer lassen, damit das Portal startet.
const char* DEFAULT_SSID = "";
const char* DEFAULT_PASSWORD = "";
const char* DEFAULT_TANKER_API_KEY = "";
const char* DEFAULT_STATION_ID = "";
const char* DEFAULT_OTA_PASSWORD = "";

#endif
